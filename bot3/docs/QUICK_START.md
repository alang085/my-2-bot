# 快速启动指南

## 🚀 启动机器人

### 标准启动流程

```bash
# 1. 进入项目目录
cd /Users/macbookair/Downloads/未命名文件夹/loan005.bot/bot3

# 2. 停止所有旧实例（如果有）
pkill -9 -f "python3 main.py"

# 3. 等待连接释放（重要！）
sleep 60

# 4. 启动机器人
python3 main.py
```

### 后台运行

```bash
# 使用 nohup
cd /Users/macbookair/Downloads/未命名文件夹/loan005.bot/bot3
nohup python3 main.py > bot.log 2>&1 &

# 或使用 screen
screen -S bot3
python3 main.py
# 按 Ctrl+A 然后 D 来分离会话
```

## ⚠️ Conflict 错误处理

如果看到 `Conflict: terminated by other getUpdates request` 错误：

### 步骤1: 彻底停止所有实例
```bash
pkill -9 -f "python3 main.py"
pkill -9 -f "main.py"
```

### 步骤2: 等待连接释放
```bash
# 等待 2-3 分钟
sleep 120
```

### 步骤3: 检查所有运行位置
- 检查所有终端窗口
- 检查其他服务器/设备
- 检查后台任务

### 步骤4: 重新启动
```bash
python3 main.py
```

## ✅ 启动成功的标志

看到以下日志表示启动成功：
```
机器人启动中... 管理员数量: 1
数据库已就绪
应用创建成功
机器人启动成功，等待消息...
Scheduler started
```

## 📊 监控运行状态

### 查看实时日志
```bash
tail -f bot_runtime.log
```

### 检查进程
```bash
ps aux | grep "[p]ython3 main.py"
```

### 停止机器人
```bash
pkill -f "python3 main.py"
```

## 🧪 测试功能

启动后，在 Telegram 中测试：
- `/start` - 启动命令
- `/report` - 查看报表（需要授权）
- `/myreport` - 查看我的报表（需要归属ID）
- `/check_permission` - 检查权限状态

